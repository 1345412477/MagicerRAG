# 移动端 UI 优化方案（6.3″ 手机优先，兼顾平板）

目标尺寸：手机竖屏约 **390px**（6.3 寸），平板约 768–1024px。
原则：先本地窄屏验收，确认后再推送；改动以追加 CSS 为主，必要时小改 JS。

## A. 导航
- A1 侧栏在 ≤720px 完全隐藏（改为滑入抽屉），桌面保持现状。
- A2 顶栏新增汉堡按钮，点击抽屉滑入；选会话 / 点遮罩 / 点外部自动收起。
- A3 顶栏多余操作（导出/分享）在窄屏收起或缩小。

## B. 对话主区
- B1 消息区左右内边距收紧，头像间距减小。
- B2 消息内图片 max-width 自适应（不超过屏宽 70%）。
- B3 输入栏一行排布不挤压，容器高度用 100dvh，防软键盘顶飞。
- B4 新会话 4 个快捷卡片、知识库下拉、推荐 chips 在窄屏换行/横向可滑。

## C. 弹层
- C1 右侧参考资料抽屉在窄屏全宽。
- C2 居中确认框、新建团队库、分块预览弹窗 padding 与尺寸适配，不溢出。

## D. 知识库页
- D1 文件表格在窄屏改为纵向卡片。
- D2 顶部操作按钮换行。

## E. 管理页 & 通用
- E1 模型管理两列表单单列。
- E2 按钮点击区 ≥44px，正文 ≥14px。

## 验收
本地用浏览器窄窗 / 开发者工具设备模拟 390×844 逐页过：对话、知识库、管理中心、各弹窗。
